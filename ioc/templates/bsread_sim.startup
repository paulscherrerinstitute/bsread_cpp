
epicsEnvSet BSREAD_HOME $(BSREAD_HOME=$(TEMPLATES)/BSREAD)

addScan .01

epicsEnvSet BSREAD_PULSEID $(SYS)-BSREAD:SIM-PULSE
epicsEnvSet BSREAD_TS_SEC $(SYS)-BSREAD:SIM-TS-SEC
epicsEnvSet BSREAD_TS_NSEC $(SYS)-BSREAD:SIM-TS-NSEC

dbLoadRecords("$(BSREAD_HOME)/bsread_sim.template","P=$(SYS)-BSREAD")

< $(BSREAD_HOME)/bsread.startup

