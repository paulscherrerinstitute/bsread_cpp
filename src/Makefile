include /ioc/tools/driver.makefile

#BUILDCLASSES += Linux
EXCLUDE_VERSIONS += 3.13
EXCLUDE_VERSIONS += 3.14.12
USR_CFLAGS += -O0
USR_CFLAGS += -DZMQ_HAVE_OPENVMS
USR_CPPFLAGS += -DZMQ_HAVE_OPENVMS

PROJECT = BSREAD
